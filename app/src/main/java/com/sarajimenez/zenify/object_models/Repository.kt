package com.sarajimenez.zenify.object_models


import com.sarajimenez.zenify.models.Task
import java.time.LocalTime
import java.time.format.DateTimeFormatter

object Repository {
    /*//Base de datos en la nube
    private val db = FirebaseFirestore.getInstance()

    // Formato para convertir LocalTime a String
    private val timeFormatter = DateTimeFormatter.ofPattern("HH:mm")

    // Lista de tareas predeterminadas
    val listTasks: MutableList<Task> = mutableListOf(
        Task(
            name = "Reunión de equipo",
            description = "Reunión para discutir los avances del proyecto y sus posibles mejoras",
            dueTime = LocalTime.of(9, 0).format(timeFormatter),  // Convertido a String
            isCompleted = true,
            color = "pastelPink",
            image = "favorite_icon_svg"
        ),
        Task(
            name = "Desarrollo del nuevo proyecto",
            description = "Desarrollar el login para nuestro proyecto",
            dueTime = LocalTime.of(11, 0).format(timeFormatter),  // Convertido a String
            isCompleted = true,
            color = "pastelBlue",
            image = "idea_icon_svg"
        ),
        Task(
            name = "Revisión de código",
            description = "Revisar el código implementado por el equipo",
            dueTime = LocalTime.of(13, 15).format(timeFormatter),  // Convertido a String
            isCompleted = false,
            color = "pastelOrange",
            image = "laptop_icon_svg"
        ),
        Task(
            name = "Llamada con cliente",
            description = "Llamada para obtener feedback del cliente",
            dueTime = LocalTime.of(14, 0).format(timeFormatter),  // Convertido a String
            isCompleted = true,
            color = "pastelMint",
            image = "phone_icon_svg"
        ),
        Task(
            name = "Comida",
            description = "Pausa para comer",
            dueTime = LocalTime.of(14, 30).format(timeFormatter),  // Convertido a String
            isCompleted = true,
            color = "pastelPurple",
            image = "bread_icon_svg"
        )
    )

    // Obtener todas las tareas
    fun getAllTasks(): List<Task> {
        return listTasks
    }

    // Agregar una tarea
    fun addTask(task: Task) {
        listTasks.add(task)
    }*/
}
