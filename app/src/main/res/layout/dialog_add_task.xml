<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">


    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Creación de tareas"

        />

    <!-- EditText para el nombre de la tarea -->
    <EditText
        android:id="@+id/edit_task_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Nombre de la tarea"
        android:inputType="text"
        tools:ignore="TouchTargetSizeCheck" />

    <!-- EditText para la descripción de la tarea -->
    <EditText
        android:id="@+id/edit_task_description"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Descripción de la tarea"
        android:inputType="text"
        tools:ignore="TouchTargetSizeCheck" />

    <!-- Campo para la hora de la tarea -->
    <TimePicker
        android:id="@+id/due_time"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:timePickerMode="clock"
        />


    <FrameLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Completada"
            android:textSize="19dp"
            android:layout_marginTop="10dp" />

        <CheckBox
            android:id="@+id/isCompletedCheckbox"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="130dp" />
    </FrameLayout>

    <!-- EditText para el nombre del archivo de la imagen -->
    <EditText
        android:id="@+id/edit_task_image_url"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Nombre de la imagen (sin extensión)"
        android:inputType="text"
        tools:ignore="TouchTargetSizeCheck" />

    <!-- Botón para seleccionar el color -->
    <Button
        android:id="@+id/select_color_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Seleccionar color"
        android:layout_marginTop="10dp" />

    <!-- Botón para guardar -->
    <Button
        android:id="@+id/save_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Guardar" />

    <!-- Botón para cancelar -->
    <Button
        android:id="@+id/cancel_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Cancelar" />
</LinearLayout>
